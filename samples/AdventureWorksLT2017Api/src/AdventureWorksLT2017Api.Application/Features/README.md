# Application features

Add use-case services, commands, queries, validators, and orchestration code here.

This folder is reserved for custom code so the generated starter can grow into a real application without mixing every hand-written change into generated files.